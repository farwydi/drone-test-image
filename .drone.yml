kind: pipeline
name: default

steps:
- name: kaniko
  image: plugins/kaniko
  settings:
    registry: http://registry.138.199.171.38.nip.io/
    username: admin
    password: ${REGISTRY_PASWORD}
    repo: foo/bar
    tags: latest
